/* Minimal config.h generated to enable Make-only local builds when Meson isn't run.
 * Meson normally generates this file via configure_file().
 * This file defines HAVE_WINPR only if WinPR was found; we default to 0.
 */
#pragma once

/* Define to 1 if WinPR (FreeRDP) is available. Leave undefined or 0 otherwise. */
#define HAVE_WINPR 0
